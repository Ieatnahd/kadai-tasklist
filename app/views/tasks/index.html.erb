<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></li>
  <% end %>
</ul>

<%= link_to '追加タスクの投稿', new_task_path %>
<%= link_to 'Logout', logout_path, method: :delete %>